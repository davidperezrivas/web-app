import host from '../../../../configuration/host';

export const usersUrlApi = {
  users: `${host}/v1/users`,
  recoverPassword: `${host}/recoverPassword`,
  updatePassword: `${host}/updatePassword`,
};
